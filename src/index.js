import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

ReactDOM.render(
  <App />,
  document.getElementById('root')
);


// Client side: react
  // > database; firebse
  // > react loading skeleton
  // tailwind

// Architectrure
  // > components, 
  // > constants, 
  // > context, 
  // > helpers,
  // > hooks,
  // > pages,
  // > lib(firebase)
  // > services (firebase functions)
  // > style (tailwinds folder)